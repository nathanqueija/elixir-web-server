# Elixir Web Server

A simple Web Server to handle request and responses

## Installation

Just run:

```elixir
iex -S mix
```

and you'll get the iex back or if you prefer run:

```elixir
mix run
```
